# План реалізації виправлень позиціонування

- [x] 1. Написати дослідницькі тести для виявлення багів (ПЕРЕД виправленням)
  - **Property 1: Fault Condition** - Виявлення проблем позиціонування
  - **ВАЖЛИВО**: Ці тести МАЮТЬ ПРОВАЛИТИСЯ на невиправленому коді - провал підтверджує існування багів
  - **НЕ намагайтеся виправити тести або код, коли вони провалюються**
  - **ПРИМІТКА**: Ці тести кодують очікувану поведінку - вони валідують виправлення, коли пройдуть після реалізації
  - **МЕТА**: Виявити контрприклади, що демонструють існування багів
  - **Підхід Scoped PBT**: Для детермінованих багів обмежити властивість конкретними випадками, що провалюються, для забезпечення відтворюваності
  
  - [x] 1.1 Тест розміщення будівель (building.h)
    - Розмістити HQ_ROME на позиції (10, 10) з texture_offset = {-192, -112}
    - Перевірити, що будівля візуально зміщена відносно тайлів (очікується провал)
    - Виміряти фактичне зміщення в пікселях (очікується ~50px зліва, ~30px зверху)
    - Перевірити, що getRect() повертає область, що не відповідає візуальному розташуванню
    - Запустити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОВАЛЮЄТЬСЯ (це правильно - доводить існування багу)
    - Задокументувати знайдені контрприклади для розуміння першопричини
    - Позначити задачу виконаною, коли тест написаний, запущений та провал задокументований
    - _Requirements: 2.1, 2.2_
  
  - [x] 1.2 Тест кліку по будівлі (building.h)
    - Симулювати клік на центр текстури Квесторію (QUESTORIUM_ROME)
    - Перевірити, що isClicked() повертає false (очікується провал через зміщену область кліку)
    - Виміряти різницю між візуальним центром та областю кліку
    - Запустити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОВАЛЮЄТЬСЯ (підтверджує проблему з областю кліку)
    - Задокументувати контрприклади
    - Позначити задачу виконаною, коли тест написаний, запущений та провал задокументований
    - _Requirements: 2.2_
  
  - [x] 1.3 Тест відображення монет (resource_display.h)
    - Відобразити панель ресурсів з panelX = 500
    - Перевірити, що moneyTextPosition.x == panelX + 280 (очікується провал)
    - Виміряти фактичну координату (очікується panelX + 680)
    - Виміряти різницю між foodTextPosition.x та moneyTextPosition.x (очікується 400px)
    - Запустити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОВАЛЮЄТЬСЯ (підтверджує зміщення на 400px)
    - Задокументувати контрприклади
    - Позначити задачу виконаною, коли тест написаний, запущений та провал задокументований
    - _Requirements: 2.3_
  
  - [x] 1.4 Тест руху юніта по ізометричній сітці (main.cpp, pathfinding.h)
    - Відправити юніта з GridCoords(5, 5) до GridCoords(15, 15)
    - Перевірити, що pathfindingManager.requestPath викликається з grid координатами (очікується провал)
    - Перевірити, що шлях слідує ізометричній геометрії (очікується провал - прямі лінії)
    - Увімкнути debug режим та перевірити візуалізацію шляху
    - Запустити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОВАЛЮЄТЬСЯ (підтверджує використання screen coords)
    - Задокументувати контрприклади (прямі лінії замість ізометричних)
    - Позначити задачу виконаною, коли тест написаний, запущений та провал задокументований
    - _Requirements: 2.4, 2.5_

- [ ] 2. Написати тести збереження існуючої поведінки (ПЕРЕД виправленням)
  - **Property 2: Preservation** - Збереження незмінної функціональності
  - **ВАЖЛИВО**: Слідувати методології observation-first
  - Спостерігати поведінку на НЕВИПРАВЛЕНОМУ коді для не-багових входів
  - Написати property-based тести, що захоплюють спостережувані патерни поведінки з Preservation Requirements
  - Property-based тестування генерує багато тестових випадків автоматично
  - Перевірити, що тести ПРОХОДЯТЬ на НЕВИПРАВЛЕНОМУ коді
  - Позначити задачу виконаною, коли тести написані, запущені та проходять на невиправленому коді
  
  - [ ] 2.1 Тест збереження руху юнітів
    - Спостерігати: юніти рухаються по прохідних тайлах без колізій на невиправленому коді
    - Написати property-based тест: для всіх прохідних тайлів юніт може рухатися без застрягання
    - Перевірити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТЬ (підтверджує базову поведінку для збереження)
    - _Requirements: 3.1_
  
  - [ ] 2.2 Тест збереження блокування тайлів будівлями
    - Спостерігати: будівлі блокують тайли для pathfinding на невиправленому коді
    - Написати property-based тест: для всіх будівель їх footprint блокує відповідні тайли
    - Перевірити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТЬ (підтверджує базову поведінку)
    - _Requirements: 3.2_
  
  - [ ] 2.3 Тест збереження відображення їжі
    - Спостерігати: число їжі відображається на panelX + 280 на невиправленому коді
    - Написати property-based тест: для всіх значень panelX їжа відображається на panelX + 280
    - Перевірити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТЬ (підтверджує правильну поведінку їжі)
    - _Requirements: 3.3_
  
  - [ ] 2.4 Тест збереження зупинки юнітів
    - Спостерігати: юніти зупиняються при досягненні цільової позиції на невиправленому коді
    - Написати property-based тест: для всіх цільових позицій юніт зупиняється в межах допустимої похибки
    - Перевірити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТЬ
    - _Requirements: 3.4_
  
  - [ ] 2.5 Тест збереження камери
    - Спостерігати: WASD, миша та зум працюють коректно на невиправленому коді
    - Написати property-based тест: для всіх команд камери позиція оновлюється правильно
    - Перевірити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТЬ
    - _Requirements: 3.5_
  
  - [ ] 2.6 Тест збереження рендерингу тайлів
    - Спостерігати: тайли використовують ізометричний рендеринг з правильним depth sorting на невиправленому коді
    - Написати property-based тест: для всіх тайлів порядок рендерингу відповідає ізометричній геометрії
    - Перевірити тест на НЕВИПРАВЛЕНОМУ коді
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТЬ
    - _Requirements: 3.6_

- [x] 3. Виправлення 1: Коректний texture_offset для будівель

  - [x] 3.1 Реалізувати обчислення правильного offset (building.h:90-150)
    - Додати метод calculateTextureOffset(BuildingType type, int footprintSize)
    - Для footprint 3x3 (HQ): обчислити центр на тайлі (1.5, 1.5) відносно position
    - Для footprint 2x2 (інші будівлі): обчислити центр на тайлі (1.0, 1.0) відносно position
    - Використати CoordinateConverter::gridToScreen() для конвертації центру в екранні координати
    - Відняти половину розміру текстури (192, 112) від екранних координат центру
    - Повернути обчислений offset
    - _Bug_Condition: isBugCondition_Buildings(building) where texture_offset == {-192, -112}_
    - _Expected_Behavior: visuallyAlignedWithTiles(building) AND clickAreaMatchesTexture(building)_
    - _Preservation: Preservation Requirements 3.1, 3.2, 3.5, 3.6_
    - _Requirements: 2.1, 2.2_
  
  - [x] 3.2 Оновити init() для використання нового методу (building.h:90-150)
    - Замінити hardcoded texture_offset = {-192, -112} на виклик calculateTextureOffset()
    - Для HQ_ROME: texture_offset = calculateTextureOffset(HQ_ROME, 3)
    - Для BARRACKS_ROME: texture_offset = calculateTextureOffset(BARRACKS_ROME, 2)
    - Для QUESTORIUM_ROME: texture_offset = calculateTextureOffset(QUESTORIUM_ROME, 2)
    - Для інших будівель: аналогічно з відповідним footprintSize
    - _Bug_Condition: isBugCondition_Buildings(building)_
    - _Expected_Behavior: texture_offset обчислюється динамічно для кожного типу будівлі_
    - _Preservation: Preservation Requirements 3.2_
    - _Requirements: 2.1, 2.2_
  
  - [x] 3.3 Перевірити, що getRect() використовує оновлений offset (building.h:127)
    - Переконатися, що getRect() читає texture_offset після оновлення
    - Перевірити, що область кліку обчислюється як: {screenPos.x + texture_offset.x, screenPos.y + texture_offset.y, textureWidth, textureHeight}
    - Не потрібно змінювати код, якщо getRect() вже використовує texture_offset
    - _Bug_Condition: clickAreaMatchesTexture(building) == false_
    - _Expected_Behavior: clickAreaMatchesTexture(building) == true_
    - _Preservation: Preservation Requirements 3.2_
    - _Requirements: 2.2_
  
  - [x] 3.4 Перевірити дослідницькі тести будівель тепер проходять
    - **Property 1: Expected Behavior** - Будівлі точно вирівняні з тайлами
    - **ВАЖЛИВО**: Повторно запустити ТІ САМІ тести з задачі 1.1 та 1.2 - НЕ писати нові тести
    - Тести з задачі 1 кодують очікувану поведінку
    - Коли ці тести проходять, це підтверджує, що очікувана поведінка задоволена
    - Запустити тест розміщення будівель з кроку 1.1
    - Запустити тест кліку по будівлі з кроку 1.2
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тести ПРОХОДЯТЬ (підтверджує, що баг виправлений)
    - _Requirements: 2.1, 2.2_
  
  - [x] 3.5 Перевірити тести збереження все ще проходять
    - **Property 2: Preservation** - Збереження незмінної функціональності
    - **ВАЖЛИВО**: Повторно запустити ТІ САМІ тести з задачі 2.1, 2.2, 2.5, 2.6 - НЕ писати нові тести
    - Запустити тест збереження руху юнітів з кроку 2.1
    - Запустити тест збереження блокування тайлів з кроку 2.2
    - Запустити тест збереження камери з кроку 2.5
    - Запустити тест збереження рендерингу тайлів з кроку 2.6
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тести ПРОХОДЯТЬ (підтверджує відсутність регресій)
    - Підтвердити, що всі тести все ще проходять після виправлення (без регресій)

- [-] 4. Виправлення 2: Коректна координата X для монет

  - [x] 4.1 Змінити координату X з 680 на 280 (resource_display.h:36)
    - Знайти рядок: DrawText(TextFormat("%d", availableMoney), (int)panelX + 680, 50, 28, {255, 255, 255, 255});
    - Замінити на: DrawText(TextFormat("%d", availableMoney), (int)panelX + 280, 50, 28, {255, 255, 255, 255});
    - Перевірити, що Y координата залишається 50 (вертикальне вирівнювання незмінне)
    - _Bug_Condition: isBugCondition_Coins(resourceDisplay) where moneyTextPosition.x == panelX + 680_
    - _Expected_Behavior: moneyTextPosition.x == panelX + 280 (як у їжі)_
    - _Preservation: Preservation Requirements 3.3_
    - _Requirements: 2.3_
  
  - [x] 4.2 Перевірити дослідницький тест монет тепер проходить
    - **Property 1: Expected Behavior** - Монети правильно вирівняні
    - **ВАЖЛИВО**: Повторно запустити ТОЙ САМИЙ тест з задачі 1.3 - НЕ писати новий тест
    - Тест з задачі 1.3 кодує очікувану поведінку
    - Коли цей тест проходить, це підтверджує, що очікувана поведінка задоволена
    - Запустити тест відображення монет з кроку 1.3
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТЬ (підтверджує, що баг виправлений)
    - _Requirements: 2.3_
  
  - [x] 4.3 Перевірити тест збереження відображення їжі все ще проходить
    - **Property 2: Preservation** - Збереження відображення їжі
    - **ВАЖЛИВО**: Повторно запустити ТОЙ САМИЙ тест з задачі 2.3 - НЕ писати новий тест
    - Запустити тест збереження відображення їжі з кроку 2.3
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТЬ (підтверджує відсутність регресій)
    - Підтвердити, що тест все ще проходить після виправлення (без регресій)

- [x] 5. Виправлення 3: Pathfinding використовує grid координати

  - [x] 5.1 Змінити виклик pathfinding на використання GridCoords (main.cpp:1537)
    - Знайти код:
      ```cpp
      Vector2 start = {(float)units[i].x, (float)units[i].y};
      Vector2 goal = {(float)units[i].target_x, (float)units[i].target_y};
      pathfindingManager.requestPath(i, start, goal, 1.0f);
      ```
    - Замінити на:
      ```cpp
      GridCoords startGrid = units[i].getGridPosition();
      GridCoords goalGrid = units[i].target_position;
      ScreenCoords startScreen = CoordinateConverter::gridToScreen(startGrid);
      ScreenCoords goalScreen = CoordinateConverter::gridToScreen(goalGrid);
      pathfindingManager.requestPath(i, {startScreen.x, startScreen.y}, {goalScreen.x, goalScreen.y}, 1.0f);
      ```
    - Переконатися, що units[i].getGridPosition() та units[i].target_position повертають GridCoords
    - _Bug_Condition: isBugCondition_Pathfinding(unit) where requestPath викликається з screen coords_
    - _Expected_Behavior: requestPath викликається з grid coords, конвертованими через CoordinateConverter_
    - _Preservation: Preservation Requirements 3.1, 3.4_
    - _Requirements: 2.4, 2.5_
  
  - [x] 5.2 (Опціонально) Додати метод конвертації GridCoords у NavigationGrid (pathfinding.h:44-50)
    - Додати метод gridCoordsToNavGrid(GridCoords gridPos, int& navGridX, int& navGridY)
    - Реалізація:
      ```cpp
      void gridCoordsToNavGrid(GridCoords gridPos, int& navGridX, int& navGridY) const {
          ScreenCoords screenPos = CoordinateConverter::gridToScreen(gridPos);
          worldToGrid((int)screenPos.x, (int)screenPos.y, navGridX, navGridY);
      }
      ```
    - Цей крок опціональний, якщо основне виправлення в main.cpp достатнє
    - _Bug_Condition: NavigationGrid не має методу для конвертації GridCoords_
    - _Expected_Behavior: NavigationGrid може конвертувати GridCoords з урахуванням ізометричної геометрії_
    - _Preservation: Preservation Requirements 3.1_
    - _Requirements: 2.4, 2.5_
  
  - [x] 5.3 Перевірити дослідницький тест pathfinding тепер проходить
    - **Property 1: Expected Behavior** - Pathfinding використовує grid координати
    - **ВАЖЛИВО**: Повторно запустити ТОЙ САМИЙ тест з задачі 1.4 - НЕ писати новий тест
    - Тест з задачі 1.4 кодує очікувану поведінку
    - Коли цей тест проходить, це підтверджує, що очікувана поведінка задоволена
    - Запустити тест руху юніта з кроку 1.4
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тест ПРОХОДИТЬ (підтверджує, що баг виправлений)
    - _Requirements: 2.4, 2.5_
  
  - [x] 5.4 Перевірити тести збереження все ще проходять
    - **Property 2: Preservation** - Збереження руху та зупинки юнітів
    - **ВАЖЛИВО**: Повторно запустити ТІ САМІ тести з задачі 2.1 та 2.4 - НЕ писати нові тести
    - Запустити тест збереження руху юнітів з кроку 2.1
    - Запустити тест збереження зупинки юнітів з кроку 2.4
    - **ОЧІКУВАНИЙ РЕЗУЛЬТАТ**: Тести ПРОХОДЯТЬ (підтверджує відсутність регресій)
    - Підтвердити, що всі тести все ще проходять після виправлення (без регресій)

- [x] 6. Checkpoint - Переконатися, що всі тести проходять
  - Запустити всі дослідницькі тести (1.1-1.4) - всі мають ПРОХОДИТИ
  - Запустити всі тести збереження (2.1-2.6) - всі мають ПРОХОДИТИ
  - Перевірити візуально в грі:
    - Будівлі точно вирівняні з тайлами
    - Клік по будівлях спрацьовує коректно
    - Монети відображаються на правильній відстані від іконки
    - Юніти рухаються по ізометричній сітці
  - Якщо виникають питання, запитати користувача
